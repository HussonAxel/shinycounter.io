export default function CurrentHuntCard({
  pokemonId,
  pokemonName,
  pokemonData,
}: {
  pokemonId: string
  pokemonName: string
  pokemonData: any
}) {
  console.log(pokemonData)
  return <div>CurrentHuntCard</div>
}
